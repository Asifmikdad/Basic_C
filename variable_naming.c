#include<stdio.h>
int main(int argc, char const *argv[])
{
    int potatoPrice;
    int examMarks;

    potatoPrice = 10;
    examMarks = 76;

    printf("Potato price is %d\n", potatoPrice);
    printf("Exam marks is %d\n", examMarks);

    return 0;
}
/*Variable naming rule:
1. Upper case letter
2. Lower case letter
3. Underscore
4. Digits 
5. Never start with digits 

Use camel case convention- it means first letter small then first letter of everyword is capital.
*/
